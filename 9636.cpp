#include <bits/stdc++.h>

using namespace std;

int main() {

    ios::sync_with_stdio(0);
    cin.tie(0);
    int a, b;
    cin >> a >> b;
    cout << ((a - 2*b > 0) ? a - 2*b : 0);
}

// Created by May 04.05.2020 at 1:11